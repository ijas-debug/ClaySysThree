//variable
let name='Leo'; //string literal
let age=30;//Number Literal
let isApproved = false; //B oolean Literal
let firstName = undefined;
let lastName = null;
console.log(name);

// variables: cannot be a reserved keyword
//should be meaningful
//cannot start with a number (1name)
//cannot contain a space or hyphen(-)
//Are case_sensitive

//constant
 const interestRate=0.3;
 interestRate=1;
 console.log(interestRate)

 //value of a variable can be changed but value of a const can't

 //object
 let person={
    name:'Tommy',
    age:30
 };

//Dot
person.name='John';
 console.log(person);